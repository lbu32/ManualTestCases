

import { CreateCustomer } from './create-customer.component';

describe('Component: CreateCustomer', () => {
  it('should create an instance', () => {
    let component = new CreateCustomer();
    expect(component).toBeTruthy();
  });
});
